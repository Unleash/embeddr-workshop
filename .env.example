# Backend base URL for the frontend. Not needed when the backend serves the
# built frontend (production) or for the default local setup; set it only
# when frontend and backend run on different origins.
VITE_BACKEND_URL=http://localhost:3001
